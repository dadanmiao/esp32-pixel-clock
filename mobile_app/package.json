{
  "name": "pixel-clock-mobile",
  "version": "1.6.1",
  "private": true,
  "description": "Android controller for the ESP32-S3 Pixel Clock terminal",
  "scripts": {
    "prepare:web": "node scripts/prepare-web.mjs",
    "sync:android": "npm run prepare:web && cap sync android",
    "build:debug": "npm run sync:android && cd android && gradlew.bat assembleDebug",
    "run:android": "npm run sync:android && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
