# 软件与显示 V2.2 迭代说明

本次迭代不修改硬件、引脚、电源拓扑和传感器接线，只升级固件显示链路、音频分析、自动场景、通信协议和 Android App。

## V2.1.1 修正

- `/api/screen` 现在按设备正面观察方向输出 LiveView。
- 修正矩阵因实际倾斜安装而导致 App 预览同时上下、左右反转的问题。
- 实体 LED 渲染、游戏坐标、流体重力和传感器方向保持不变。

## V2.1.2 修正

- 修复 App 主颜色已下发、但被彩虹样式或各模式固定配色覆盖的问题。
- 选择主颜色时，Clock 会自动切换到 Classic，Spectrum 和 Text 会自动关闭彩虹覆盖。
- 主颜色现已用于时钟、流体、计时、天气文字、游戏主体以及全部非彩虹音频效果。
- 天气图标、计时结束、暂停提示、游戏目标和失败状态仍保留语义颜色。
- Android App 升级到 `1.2`，`versionCode 3`，选色后会显示应用成功提示。

## V2.2.0 修正

- App 调节亮度时会自动关闭自动亮度，手动亮度不再被 LDR 立即覆盖。
- API 新增 `effectiveBrightness`，App 同时显示实际亮度和当前供电安全上限。
- 手动亮度使用更快的渐变速度；设备状态 WebSocket 推送间隔缩短至 250 ms。
- App 切换模式或参数后立即更新本地预览，并在 40 ms 后请求新画面；LiveView 固定每 500 ms 刷新。
- Text 彩虹色提升亮度和饱和表现，在低亮度供电策略下仍更清晰。
- 内置场景在应用时关闭智能场景，避免手动选择被自动规则立即覆盖。
- App 新增最多 12 个自定义场景，可保存当前参数、应用、改名和删除。
- 自定义场景保存在手机本地；同名保存会更新已有场景。
- Android App 升级到 `1.3`，`versionCode 4`，可覆盖安装 1.2。

## 已完成

### 1. 统一显示合成

- 所有显示模式统一经过目标帧和输出帧。
- 支持淡入淡出、横向擦除和像素溶解三种转场。
- 模式、时钟主题、频谱子模式、天气样式和游戏类型变化时自动触发转场。
- 亮度变化使用渐进调整，减少 LDR 波动或电源策略变化造成的突跳。

### 2. 色彩和夜间显示

- 新增可关闭的 Gamma 校正。
- 智能场景处于安静时段或低照度环境时，自动限制最大亮度。
- 夜间对蓝色和绿色分量做温和抑制，降低刺眼感。
- 保留原来的供电模式亮度上限和 FastLED 电流限制。

### 3. 音频分析

- 新增自适应麦克风噪声底。
- 新增自动增益，弱音乐更容易显示，安静时不会无限放大噪声。
- 32 列频谱改为近似对数频带，更符合低频细分、高频压缩的听觉分布。
- 上升沿和下降沿采用不同平滑速度，鼓点响应更快，余辉更自然。
- API 新增噪声底、实际增益和有效信号状态。

### 4. 智能场景

- 默认关闭，避免升级后改变原有操作习惯。
- 开启后可依据计时状态、安静时段、低照度、音乐活动和姿态活动选择有效模式。
- 手动选择文字、天气和游戏时不会被普通环境事件抢占。
- API 同时返回用户选择模式和当前有效模式。

### 5. 通知覆盖层

- 新增固定长度通知队列，不使用动态容器。
- 通知以滚动覆盖层显示，不会改变当前显示模式。
- 通知开始和结束带淡入淡出。
- 番茄钟或倒计时结束后自动加入 `TIME UP` 通知。
- 新增 `POST /api/notify`。

请求示例：

```json
{
  "text": "FOCUS TIME",
  "color": {"r": 255, "g": 184, "b": 107},
  "durationMs": 6000,
  "speedMs": 72
}
```

### 6. 实时通信

- 新增 WebSocket `ws://设备地址/ws`。
- 有客户端连接时，设备每 500 ms 主动推送完整状态。
- 原有 `GET /api/state` 保留，用于首次连接和降级轮询。

### 7. 跨任务状态隔离

- 音频、环境、电源、天气、控制、游戏、场景和通知分别写入自己的状态分区。
- 完整状态写入只用于启动初始化，避免并发任务使用旧快照覆盖新数据。
- 渲染队列仍传递统一快照，显示任务的消费方式保持不变。

### 8. Android App 1.2

- 新增设备地址快速查找。
- 新增实时/轮询连接状态。
- 新增智能场景、安静时段、夜间亮度、转场和色彩校正控制。
- 新增音频自动增益控制。
- 文字页新增临时通知，不必切换到 Text 模式。
- APK 的 `versionCode` 已提升到 3，可覆盖安装旧版。

## 默认值

| 参数 | 默认值 |
|---|---:|
| 智能场景 | 关闭 |
| 平滑转场 | 开启 |
| 转场样式 | 淡入淡出 |
| 转场时间 | 320 ms |
| Gamma 校正 | 开启 |
| 音频自动增益 | 开启 |
| 安静时段 | 23:00 到 07:00 |
| 夜间亮度上限 | 22 |

## 兼容性

- NVS 设置版本从 1 升级到 2。
- 固件能够读取版本 1 的现有配置，并为新字段补充默认值。
- 原有 HTTP 控制接口、模式编号和显示参数保持兼容。
- 硬件 GPIO 和电源保护策略未改变。

## 构建验证

- PlatformIO 固件构建通过。
- 固件 RAM 使用约 19.9%，Flash 使用约 34.4%。
- Android Debug APK 构建通过。
- Android 包名：`com.pixelclock.mobile`
- Android 版本：`1.3`，`versionCode 4`
- APK Signature Scheme v2 签名验证通过。
# v2.4.0

- 新增 30 分钟本地桌面状态时间线，状态变化会立即写入轨迹。
- 新增人工真值标注、四类混淆矩阵和默认模型/个性化模型准确率对比。
- 新增离线本地推理计数，作为断网仍可工作的比赛演示证据。
- App 升级至 v1.5，提供验证样本记录、评估重置与时间线可视化。

# v2.3.0

- 新增 ESP32-S3 本地桌面状态分类：专注、会议、休息、离桌。
- 新增五维可解释特征、置信度、类别得分与推理耗时 API。
- 新增 App 内个性化校准与 NVS 样本持久化。
- 新增可选 AI 驱动场景和 32x8 状态指示像素。
